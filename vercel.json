{
  "builds": [
    { "src": "api/server.js", "use": "@vercel/node" },
    { "src": "frontend/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(auth|clientes|aumentos|pagos)/(.*)", "dest": "/api/server.js" },
    { "src": "/api/(.*)", "dest": "/api/server.js" },
    { "src": "/(.*)", "dest": "/frontend/index.html" }
  ]
}
